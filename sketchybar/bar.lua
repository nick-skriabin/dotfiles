local colors = require("colors")

-- Equivalent to the --bar domain
--
sbar.bar({
	blur_radius = 30,
	color = colors.base,
	corner_radius = 8,
	margin = 0,
	notch_width = 0,
	padding_left = 10,
	padding_right = 10,
	position = top,
	height = 48,
	shadow = on,
	sticky = on,
	topmost = off,
	y_offset = 0,
})
